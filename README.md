# sentiment-analysis-distilbert-chromadb
A containerized NLP app combining sentiment analysis (DistilBERT) and semantic search (ChromaDB + Sentence-BERT). Features a Gradio UI for single/batch/file inputs and a FastAPI backend. Docker Compose orchestrates all services—run with one command for cross-platform use. Ideal for analyzing reviews/feedback and finding contextually similar texts.
